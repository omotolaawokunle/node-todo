<!DOCTYPE html>
<html>
  <head>
    <title>Login</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <div class="flex flex-col items-center justify-center h-screen border border-gray-300 rounded-md p-4">
        <h1 class="text-2xl font-bold">Login</h1>
        <p class="text-sm text-gray-500">Welcome to Login</p>
        <form action="/login" method="post" class="flex flex-col gap-2 mb-2">
        <span class="text-red-500 text-sm"><%= errors?.message ? errors.message : '' %></span>
        <div class="flex flex-col gap-2 mb-2">
            <label for="username">Username</label>
            <input type="text" name="username" placeholder="Username" id="username" required class="border border-gray-300 rounded-md p-2" />
        </div>
        <div class="flex flex-col gap-2 mb-2">
            <label for="password">Password</label>
            <input type="password" name="password" placeholder="Password" id="password" required class="border border-gray-300 rounded-md p-2" />
        </div>
        <button type="submit" class="bg-blue-500 text-white p-2 rounded-md">Login</button>
        </form>
        <p>Don't have an account? <a href="/register" class="text-blue-500">Register</a></p>
    </div>
  </body>
</html>
