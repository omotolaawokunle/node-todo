<!DOCTYPE html>
<html>
  <head>
    <title>Register</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <div class="flex flex-col items-center justify-center h-screen border border-gray-300 rounded-md p-4 max-w-lg mx-auto">
    <h1 class="text-2xl font-bold">Register</h1>
    <p class="text-sm text-gray-500">Welcome to Register</p>
    <form action="/register" method="post" class="flex flex-col gap-2 mb-2">
      <div class="flex flex-col gap-2 mb-2">
        <label for="username">Username</label>
        <input type="text" name="username" placeholder="Username" id="username" required class="border border-gray-300 rounded-md p-2" value="<%= username ? username : '' %>" />
        <span <%= errors?.username ? 'class="text-red-500 text-sm"' : 'class="hidden"' %>><%= errors?.username ? errors.username : '' %></span>
      </div>
      <div class="flex flex-col gap-2 mb-2">
        <label for="password">Password</label>
        <input type="password" name="password" placeholder="Password" id="password" required class="border border-gray-300 rounded-md p-2" value="<%= password ? password : '' %>" />
        <span <%= errors?.password ? 'class="text-red-500 text-sm"' : 'class="hidden"' %>><%= errors?.password ? errors.password : '' %></span>
      </div>
      <button type="submit" class="bg-blue-500 text-white p-2 rounded-md">Register</button>
    </form>
    <p>Already have an account? <a href="/login" class="text-blue-500">Login</a></p>
    </div>
  </body>
</html>
